## What does this change?

<!-- One or two sentences on what changed and why. -->

## Related issue

<!-- Closes #... , or "N/A" if this wasn't discussed in an issue first. -->

## How was this tested?

<!-- e.g. ran `extract` against sample-report.md, ran `apply` in dry-run,
     tested `apply --apply --only BUG-01` against a test Taiga project. -->

## Checklist

- [ ] Code and user-facing strings are in English
- [ ] `README.md` and `README.pt-BR.md` updated if behavior/flags/config format changed
- [ ] Added an entry to `CHANGELOG.md` under `Unreleased`
- [ ] No `.env`, `config.json`, or other real credentials/data included in the diff
